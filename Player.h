#include "Pawn.h"
#include "Task.h"
#include <vector>

class Player {
public:
    Building* hub;
    std::vector<Pawn*> pawns;
    void addPawn(Pawn* pawn){
        //TODO
    }
    void assignTask(TaskID id) {
        //TODO
    }
};
